kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: cluster-a

nodes:
  - role: control-plane
    extraPortMappings:
      # Porta para expor Ingress / API Gateway
      - containerPort: 30082
        hostPort: 32072
        protocol: TCP

      # Porta opcional para métricas, debug, etc
      - containerPort: 30092
        hostPort: 32092
        protocol: TCP