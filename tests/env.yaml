clusters:
  cluster-a:
    name: system-tests-lab-a
    kubeContext: kind-system-tests-lab-a
    kindConfig: tests/infra/kind/cluster-a.yaml

  cluster-b:
    name: system-tests-lab-b
    kubeContext: kind-system-tests-lab-b
    kindConfig: tests/infra/kind/cluster-b.yaml

localstack:
  release: "localstack"
  namespace: "localstack"

images:
  nats:
    image: "nats:2.12-alpine"
  redis:
    image: "redis:7-alpine"
  postgres:
    image: "postgres:16-alpine"

timeouts:
  createCluster: 2m
  apply: 2m